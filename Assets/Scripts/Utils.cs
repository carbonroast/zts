using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum TileType
{
    Water,
    Grassland,
    Plains,
    Mountain
};

public enum State
{
    Idle,
    Building,
    Selecting
};